import React from 'react';

/** Full-width 1px separator. Drop between data rows or between sections. */
export function RowDivider() {
  return <div style={{ width: '100%', height: 1, background: 'rgba(255,255,255,0.08)' }} />;
}
